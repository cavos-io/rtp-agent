version: 3
workdir: .

allow:
  depOnAnyVendor: false
  deepScan: true

exclude:
  - .codex
  - .git
  - .tmp
  - docs
  - refs
  - resources
  - worktrees

excludeFiles:
  - ".*_test\\.go"

components:
  app:
    in: app

  cmd:
    in: cmd

  examples:
    in: examples/**

  interface:
    in: interface/**

  adapter:
    in: adapter/**

  core:
    in:
      - core/**

  library:
    in: library/**

  scripts:
    in: scripts/parity-runners/**

commonComponents:
  - library

deps:
  cmd:
    anyVendorDeps: true
    mayDependOn:
      - app
      - interface

  examples:
    anyVendorDeps: true
    mayDependOn:
      - app
      - core
      - interface
      - library

  app:
    anyVendorDeps: true
    mayDependOn:
      - adapter
      - core
      - interface
      - library

  interface:
    anyVendorDeps: true
    mayDependOn:
      - core
      - interface

  adapter:
    anyVendorDeps: true
    mayDependOn:
      - core
      - adapter

  core:
    anyVendorDeps: true
    mayDependOn:
      - core

  library:
    anyVendorDeps: true
    mayDependOn:
      - library

  scripts:
    anyVendorDeps: true
    mayDependOn:
      - library
