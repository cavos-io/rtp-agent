This directory contains repository-owned audio fixtures used by Go tests.

`long.mp3` is a 22.05 kHz mono MP3. Keep tests independent from mutable
fixtures checked out under the ignored `refs/` directory.
