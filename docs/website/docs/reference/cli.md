---
id: cli
title: CLI
---

# CLI reference

Status: **implemented**.

Evidence:

- `interface/cli/cli.go`
- `interface/cli/cli_test.go`
- `interface/cli/watcher.go`

The CLI supports modes such as `start`, `dev`, `console`, and `download-files`, plus worker options including URL, API key, API secret, log level, reload, and drain timeout.

